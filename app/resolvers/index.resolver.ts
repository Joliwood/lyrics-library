import {
  Album,
  Artist,
  ArtistLikeSong,
  Mutation,
  Query,
  Song,
  SongOnAlbum,
} from '#resolvers';

export default {
  Album,
  Artist,
  ArtistLikeSong,
  Mutation,
  Query,
  Song,
  SongOnAlbum,
};
