export default {
    // async albums(parent, _, { dataSources }) {
    //   const rows = await dataSources.lyricsLibrary.artist.getAlbums(parent.id);
    //   return rows;
    // },
  
    // async songs(parent, _, { dataSources }) {
    //   const rows = await dataSources.lyricsLibrary.artist.getSongs(parent.id);
    //   return rows;
    // },
  };
  