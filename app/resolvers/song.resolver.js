import songDatamapper from '../datamappers/song.js';

export default {
    // async album(parent, _, { dataSources }) {
    //   const row = await dataSources.lyricsLibrary.song.getAlbum(parent.album_id);
    //   return row;
    // },
  
    // async artist(parent, _, { dataSources }) {
    //   const row = await dataSources.lyricsLibrary.song.getArtist(parent.album_id);
    //   return row;
    // },
  
  // Query: {
  
  //   songs: async () => {
  //     const rows = await songDatamapper.findAll();
  //     return rows;
  //   }
      
  // }
  };
  