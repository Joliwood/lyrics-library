export default {
    // async artist(parent, _, { dataSources }) {
    //   const row = await dataSources.lyricsLibrary.album.getArtist(parent.artist_id);
    //   return row;
    // },
  
    // async songs(parent, _, { dataSources }) {
    //   const rows = await dataSources.lyricsLibrary.album.getSongs(parent.id);
    //   return rows;
    // },
  };
  