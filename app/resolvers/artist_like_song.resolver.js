export default {
  // If we would like to have everyone who liked a song async song(parent, _, {
  // dataSources }) { const row = await
  // dataSources.lyricsdb.artistLikeSongDatamapper.idsLoader.load(parent.song_id);
  // return row;
  // },
};
