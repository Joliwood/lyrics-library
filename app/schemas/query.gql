type Query {
  albums: [Album]
  album(id: Int!): Album
  artists: [Artist]
  artist(id: Int!): Artist
  songs: [Song]
  song(id: Int!): Song

  # Request that allow a JsonWebToken recuperation
  login(input: LoginInput!): JWT

  profile: ArtistUser
}
