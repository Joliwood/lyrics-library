type Query {
  album(id: Int!): Album
  albums: [Album]
  artist(id: Int!): Artist
  artists: [Artist]
  song(id: Int!): Song
  songs(limit: Int, filter: SongFilterInput): [Song]
  login(input: LoginInput!): JWT
  profile: ArtistUser
}
